# Do not hard-fail `pnpm run` when optional build scripts are ignored;
# allowed builds are declared in pnpm-workspace.yaml (onlyBuiltDependencies).
strict-dep-builds=false
verify-deps-before-run=false
